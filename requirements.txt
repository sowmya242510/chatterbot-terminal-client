# Manifest of dependencies for the ChatterBot + Django terminal chat client.
#
# IMPORTANT COMPATIBILITY NOTE:
# ChatterBot (last released 2020) was written for older Django/SQLAlchemy
# versions and has not been updated since. It reliably works on:
#   - Python 3.7 or 3.8 (Python 3.9+ frequently breaks its dependencies)
#   - Django 2.x
# Using a virtual environment with Python 3.8 is strongly recommended.

Django>=2.1,<3.0
chatterbot==1.0.5
chatterbot-corpus==1.2.0
